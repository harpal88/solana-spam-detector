/**
 * Solana Spam Detector Server
 * 
 * This is the main entry point for the Solana Spam Detector application.
 * It starts the API server and serves the dashboard.
 */

// Start the API server
require('./src/api/api-server');

console.log('Solana Spam Detector server started');
