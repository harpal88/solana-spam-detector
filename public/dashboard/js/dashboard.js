/**
 * Extremely simplified dashboard.js
 * This file contains only the essential functionality for the dashboard
 */

// Wait for the DOM to be fully loaded
document.addEventListener('DOMContentLoaded', function() {
  console.log('Dashboard initialized');
  
  // Initialize Bootstrap tabs
  // This relies on Bootstrap's built-in tab functionality
  // No custom JavaScript needed for basic tab switching
});
